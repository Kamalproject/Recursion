In recursion repository i can solve the all problems using recursion which programmes are already solved in DSA repository. 
you can Go through this recursion repository as well as DSA repository also.
